<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:supportsRtl="true">
		<!-- Der BackgroundLocationService wird ueber das [Service]-Attribut in der Klasse deklariert,
		     damit der ACW-Name immer stimmt. -->
	</application>
	<uses-permission android:name="android.permission.BATTERY_STATS" />
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<!-- Ohne diesen Eintrag bietet Android im Freigabedialog gar kein "Immer erlauben" mehr an und
	     Permissions.LocationAlways wirft in MAUI eine PermissionException (das hat die App beim
	     Start der Aufnahme abgestuerzt). Aufgezeichnet wird auch mit "Beim Verwenden", weil der im
	     Vordergrund gestartete location-Foreground-Service weiterlaufen darf; der Eintrag ist die
	     Voraussetzung dafuer, dass der Nutzer in den Systemeinstellungen "Immer erlauben" waehlen
	     kann. Ab Android 11 zeigt der Dialog die Option nicht mehr, dafuer fuehrt die
	     Einstellungen-Seite der App mit IAppSettings direkt auf die Berechtigungsseite. -->
	<uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
	<uses-feature android:name="android.hardware.location.gps" android:required="false" />
	<uses-feature android:name="android.hardware.location.network" android:required="false" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
	<uses-sdk />
</manifest>